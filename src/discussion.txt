Java
-----------
Sun-MicroSystem
|
Oracle

Inheritance
-------------

Parent Class/ Base class / Super Class
Child Class / Sub Class / Derived Class / Extended Class

Object

public class Parent {
}

public class Child extends Parent {
}


Bicycle
-> gear, cadence, speed

MountainBike
-> seatHeight


TODO
-------------------
this->
new ->
protected -> ক্লিয়ার না
super -> বুঝতে পারি নাই
constructor ->


TODO
-----------
git clone -> remote repository theke PC te repository ta download kore/clone kore.
git clone "URL"

git add -> clear
git add . [ja ja change ase sob add hobe]
git add filename

git commit -> clear
git commit -m "message dite hobe"

git push -> clear
git push origin branch_name

git pull -> clear
git pull origin branch_name
git pull [repository te ja ase sob pull hoye chole asbe]

git branch -> clear
git branch [pc te joto branch ase sob list dekhabe]

git checkout -> clear
git checkout -b branch_name [new branch create hobe]
git checkout branch_name [branch switch]

git status -> ai muhurte git r obostha
git status

git log -> commit r history
git log

git merge ->
branch conflict ->
git commit revert ->
git stash ->
----------------------------------



Java
----------------------------------
Class ->
Object ->
new ->
this ->
access modifier ->
public ->
private ->
protected ->
default ->
constructor ->
this ->
inheritance ->
super ->
method override ->
method overloading ->
abstract method ->
abstract class ->
object cloning ->
this() ->
constructor chaining -> super(), this()

interface ->

---------------
Object -> 10
library class -> 3790 * 10 = 37900
---------------